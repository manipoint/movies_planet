import 'package:flutter/material.dart';

class PopularShowsView extends StatelessWidget {
  const PopularShowsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("PopularTVShowsView"),
    );
  }
}
